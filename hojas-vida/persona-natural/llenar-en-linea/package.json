{
  "name": "formatounicopdf",
  "version": "1.0.0",
  "private": true,
  "description": "Generador y vista previa del Formato Único de Hoja de Vida (PDF) - scripts de build para producción",
  "scripts": {
    "clean": "rimraf dist",
    "build:js": "terser script.js -c -m -o dist/script.min.js",
    "build:css": "cleancss -o dist/styles.min.css styles.css",
    "build:copy": "cpy formatounico.pdf dist && cpy CNAME dist || true",
    "build": "npm run clean && mkdir -p dist && npm run build:js && npm run build:css && npm run build:copy",
    "serve": "npx http-server dist -p 5500"
  },
  "devDependencies": {
    "terser": "^5.14.2",
    "clean-css-cli": "^5.6.3",
    "cpy-cli": "^3.1.1",
    "rimraf": "^5.0.0"
  }
}
